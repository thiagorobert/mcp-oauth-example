{
	"mcpServers": {
		"github_oauth_example": {
			"command": "uv",
			"args": [
				"--directory",
				"/workspace/github/mcp-oauth-example",
				"run",
				"mcp_github.py",
				"--token",
				"${GITHUB_TOKEN}"
			],
			"env": {
				"GITHUB_TOKEN": "${GITHUB_TOKEN}"
			}
		}
	}
}